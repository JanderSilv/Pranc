export enum VRF {
    low = 0,
    medium,
    high,
    none
}

export enum VSL {
    lower = 0,
    moderate,
    high,
    severe,
    none
}

export enum ApplicabeSystem{
    lowImpact = 0,
    mediumImpact,
    HighImpact,
    none
}

export enum QuestionType{
    single,
    multiple,
    date,
    number
}
