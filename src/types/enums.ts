export enum VRF {
  low = 0,
  medium,
  high,
  none,
}

export enum VSL {
  lower = 0,
  moderate,
  high,
  severe,
  none,
}

export enum ApplicableSystem {
  lowImpact = 0,
  mediumImpact,
  HighImpact,
  none,
}

export enum QuestionType {
  single = 'radio',
  yesNo = 'yes-no',
  multiple = 'checkbox',
  date = 'date',
  number = 'number',
}
